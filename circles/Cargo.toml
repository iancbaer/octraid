[workspace]
members = [
    "rust_circle_sdk",
    "identity-registry",
    "reputation-registry",
    "mandate-registry",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
strip = true
